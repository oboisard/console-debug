import { createDefaultEntries } from '@oboisard/rollup-toolkit';

/**
 * @type {Array<import('rollup').CustomPluginOptions>}
 */
export default createDefaultEntries({
  hasScss: false,
  hasAssetsCopy: false,
});
